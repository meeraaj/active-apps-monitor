graph TD
    subgraph Client [Client Device]
        Monitor[App Monitor Script]
        LocalLogs[Local Log Files]
        Monitor -->|Writes| LocalLogs
        LocalLogs -->|Zips & Uploads| Azure
    end

    subgraph Cloud [Cloud Infrastructure]
        Azure[Azure Blob Storage]
    end

    subgraph Server [Backend Server]
        API[Flask API]
        Parser[Log Parser]
        DB[(SQLite Database)]
        
        Azure -->|Downloads| API
        APIgraph TD
    subgraph Client [Client Device]
        Monitor[App Monitor Script]
        LocalLogs[Local Log Files]
        Monitor -->|Writes| LocalLogs
        LocalLogs -->|Zips & Uploads| Azure
    end

    subgraph Cloud [Cloud Infrastructure]
        Azure[Azure Blob Storage]
    end

    subgraph Server [Backend Server]
        API[Flask API]
        Parser[Log Parser]
        DB[(SQLite Database)]
        
        Azure -->|Downloads| API
        API